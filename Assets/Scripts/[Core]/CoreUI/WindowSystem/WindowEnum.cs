namespace GameKit.UI
{
    /// <summary>
    /// енмчик для типов окон
    /// </summary>
    public enum WindowEnum
    {
        HeroScreen,
        Game,
        PauseMenu,
        UserSettings,
        LoadScreen,
        Develop
    }

    public enum WindowState { Open, Closed }
}